[build]
  publish = "public"
  command = "npm run check"
[build.environment]
  NODE_VERSION = "22"
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["express", "pg", "exceljs", "serverless-http"]
  included_files = ["*.js"]
